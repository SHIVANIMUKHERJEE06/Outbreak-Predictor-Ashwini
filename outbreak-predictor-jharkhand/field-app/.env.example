# Copy this file to .env for local development, or set the same
# variable in Vercel's project settings for the deployed version.
#
# For local dev (backend running on your machine):
VITE_API_BASE_URL=http://localhost:8000

# For production (after deploying the backend to Render), change this to:
# VITE_API_BASE_URL=https://your-backend-name.onrender.com
